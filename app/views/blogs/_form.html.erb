<div class="grid_12">
	<%= form_for([@user,@blog], :html => { :multipart => true }) do |f| %>
	  <% if @blog.errors.any? %>
	    <div id="error_explanation">
	      <h2><%= pluralize(@blog.errors.count, "error") %> prohibited this blog from being saved:</h2>
	
	      <ul>
	      <% @blog.errors.full_messages.each do |msg| %>
	        <li><%= msg %></li>
	      <% end %>
	      </ul>
	    </div>
	  <% end %>
	
	
	<div class="field">
	  <%= t('system.title') %><br />
	  <%= f.text_field :title %>
	</div>
	<div class="field">
	  <%= t('system.body') %><br />
	  <%= f.text_area :body %>
	</div>
	
	<div class="field">
		<%#= t('user.language')%><br/>
		<%#= f.select(:language, User::LANGUAGE)%>
		<%= f.hidden_field(:language)%>
	</div>
	  
	  <div class="actions">
	    <%= f.submit t('admin.save')%>
	  </div>
	<% end %>
</div>
