<div class="grid_12">
	<%= form_for([@tutorial,@tutorial_post], :html => { :multipart => true }) do |f| %>
		<%#= form_for(@tutorial_post) do |f| %>
		<% if @tutorial_post.errors.any? %>
		  <div id="error_explanation">
		    <h2><%= pluralize(@tutorial_post.errors.count, "error") %> prohibited this tutorial_post from being saved:</h2>
		
		    <ul>
		    <% @tutorial_post.errors.full_messages.each do |msg| %>
		      <li><%= msg %></li>
		    <% end %>
		    </ul>
		  </div>
		<% end %>
		
		<div class="field">
		  <%= f.label :title %><br />
		  <%= f.text_field :title %>
		</div>
		<div class="field">
		  <%= f.label :body %><br />
		  <%= f.text_area :body %>
		</div>
		<div class="actions">
		  <%= f.submit %>
		</div>
	<% end %>

</div>
